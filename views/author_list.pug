extends layout

block content
  h1= title

  ul
    each author in listOfAuthors
      li
        a(href=author.url) #{`${author.family_name} ${author.first_name}`}
        | (#{author.formattedDate.DOB} - #{author.formattedDate.DOD})

    else
      li There are no authors.
